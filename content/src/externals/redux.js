// redux is a vendor global (loaded from /vendor/redux-v4.js).
export const bindActionCreators = window.Redux.bindActionCreators;
export const createStore = window.Redux.createStore;
export const combineReducers = window.Redux.combineReducers;
export const applyMiddleware = window.Redux.applyMiddleware;
