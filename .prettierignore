# Overrides applied on top of .gitignore by the format wrapper
# (3rd-party/automation/run-format.js), merged into one ignore set with
# single-file gitignore semantics (last matching rule wins).

# Third-party vendored libraries and automation tooling - not our code.
/vendor/
/3rd-party/

# Verbatim vendored weh framework (content/src/weh) - kept byte-for-byte identical
# to upstream, so prettier must not reformat it. See content/src/weh/README.md.
/content/src/weh/

# Verbatim vendored Tooltip + ViewportMetrics (content/src/tooltip) - byte-for-byte
# copies of the transcoder-manager components, so prettier must not reformat them.
/content/src/tooltip/

# Unreferenced minified test fixture (also excluded from the naming audit).
/tests/main.js

# Vendored Shoelace assets (icons + themes) that setBasePath points at.
/content2/shoelace/
